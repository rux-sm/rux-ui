# Treat package.json "engines" as a requirement, not a suggestion. Without
# this, npm prints a warning on an unsupported Node version and carries on,
# which is how a machine ends up producing output nobody can reproduce.
engine-strict=true
